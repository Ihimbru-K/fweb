// Public API for the 'orders' feature slice.
// Only export what other features/pages actually need.
// Do NOT import from 'orders/components/*', 'orders/hooks/*' etc. directly
// outside this slice — that is an architecture violation (see Doc 02, s.3).

export {};
